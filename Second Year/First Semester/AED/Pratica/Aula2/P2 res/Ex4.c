#include <assert.h>

int main(void){
    int a[] = {1,2,3,4,5,6,7,8};
    int l = sizeof(a) / sizeof(a[0]);
    assert(a != null && l > 0);
    for (int i = 0; i < l; i++){
        printf("%d", n[i]);
    }
}

void DisplayArray(double* a, size_t n) {

    
}
